.center-area(data-editable='false')
  header.center-header
    .view-path Termini reprodukcije
  .center-content
    header.content-header
      .content-header-left
        .content-header-title Termini reprodukcije
        .content-header-subtitle
          | Pregledajte svoje termine i
          | sastavite liste za reprodukciju odabrane termine, te pošaljite zahtjeve za dodjelom drugih termina.
      .content-header-right
        i.material-icons.icon schedule
    .content-primary.content-calendar(data-status='change')
      .calendar-wrapper
        .calendar.large-calendar
          table( style="width: 100%; height: 100%")
            tbody
              tr
                th( '*ngFor'="#day of days" style="border: 1px solid #1976D2" ) {{day}}
              tr( '*ngFor'= "#hour of hours")
                td( '*ngFor'="#day of daysNum" style="border: 1px solid #1976D2" '(click)'="slotClicked(day, hour)") {{day}} {{hour}}
      .calendar-actions
        button Zatraži novi termin
      .calendar-option
        form('[ngFormModel]'="requestForm" '(ngSubmit)'="requestSlot(requestForm.value)")
          h3 Zatraži termin
          .form-line
            label(for='time') Vrijeme
            select(name='for' '[ngFormControl]'="requestForm.controls['time']")
              option( '*ngFor'="#hour of hours" '[value]'="hour") {{hour}}:00
          .form-line
            label Dani ponavljanja
            fieldset.day-checks
              input(type='checkbox', '*ngFor'="#day of daysNum" '[ngFormControl]'="requestForm.controls['day'+day]")
              ul.check-labels
                li P
                li U
                li S
                li Č
                li P
                li S
                li N
          .form-line
            label(for='start_date') Početni datum
            input(type='date', name='start_date' '[ngFormControl]'="requestForm.controls['start_date']")
          .form-line
            label(for='end_date') Završni datum
            input(type='date', name='end_date' '[ngFormControl]'="requestForm.controls['end_date']")
          .form-line
            button(type="submit").raised Zatraži termin
